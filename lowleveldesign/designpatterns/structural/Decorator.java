package designpatterns.structural;

/**
 * Decorator pattern lets you dynamically change the behavior of an object at run time by wrapping
 * them in an object of a decorator class.
 * Decorator - Attach additional responsibilities to an object dynamically.
 * It allows for the dynamic addition of responsibilities to objects without modifying their code.
 * Uses IS-A and HAS-A
 */
public class Decorator {

    // shadab git good example
}
