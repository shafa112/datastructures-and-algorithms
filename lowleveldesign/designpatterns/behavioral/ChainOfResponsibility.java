package designpatterns.behavioral;

/**
 * it is concerned with how objects interact and communicate with each other.
 * Specifically, it allows for the processing of a request by multiple objects, forming a chain.
 * Each object in the chain either handles the request or passes it to the next object in the chain.
 */
public class ChainOfResponsibility {
}
